function chatMessages(state={}, action) {
  switch(action.type) {
    case 'X':
      return state
    case 'X':
      return state
    default:
      return state;
  }
}

export default chatMessages;
