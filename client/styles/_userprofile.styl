@import '_colorsfontsstyles.styl'

#user-pic
  display block
  margin 0 auto
  width 12.25rem
  height 12.25rem
  border 1px solid navBarNavy
  box-shadow: 0px 1px 1px standardGrey
  background-image url(../data/user-silhouette.png)
  background-repeat no-repeat
  border-radius 50%
  
#user-data
  margin-top .5rem
  text-align center
  #username
  #user-email
  #user-created-date
    :first-child
      font-family lobster
      font-size 1.3rem
      color navBarNavy
    :last-child
      font-family blocky
      font-size 2rem
      color logoLightBlue
  form
    input
      font-family blocky
      font-size 2rem
      margin-top 1.3rem
  button
    background-color white
    border 2px solid navBarNavy
    padding 0 1rem
    margin .2rem 0 1rem
    font-family arial
    font-size .8rem
  button:hover
    background-color navBarNavy
    color white
    transition-delay 0s
    transition-duration 0.3s
    transition-property all
    transition-timing-function ease
  #most-popular-link
    margin-top 1.5rem
    font-family arial
    font-size 1.3rem
    color logoLightBlue
    :first-child
      font-family blocky
      color navBarNavy
      